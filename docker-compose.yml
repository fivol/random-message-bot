version: "3.3"
volumes:
  postgres:

services:
  app:
    build: .
    restart: always
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: random_message
    volumes:
      - postgres:/var/lib/postgresql/data
