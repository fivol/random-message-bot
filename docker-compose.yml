version: "3.3"
services:
  app:
    build: .
    restart: always
    network_mode: host
    environment:
      BOT_API_TOKEN: ***REMOVED***
      PG_URL: postgresql://postgres:***REMOVED***@db.example.com:5432